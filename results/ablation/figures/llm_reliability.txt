What the model layer actually did across the whole experiment: cache hits against live calls, and every schema failure, transport failure and fallback. A cached run reproduces these numbers without an API key.
