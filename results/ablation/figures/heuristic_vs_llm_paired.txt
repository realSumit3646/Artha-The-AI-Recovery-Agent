The deciding comparison: per-seed net recovery delta, LLM agent minus heuristic agent. The LLM agent lost on 33 of 120 seeds (27.5%). Both arms share the same scheduler, validator and cost model; the only difference is whether a model resolves residual diagnoses and picks the intervention.
