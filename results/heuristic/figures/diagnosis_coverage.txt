What a rule-based code book can and cannot resolve. 23.0% of failures return UNKNOWN — a generic code, a missing code, or a funds code the customer's payment history cannot separate from a ceiling breach. That share is the honest size of the gap a model stage would have to fill.
